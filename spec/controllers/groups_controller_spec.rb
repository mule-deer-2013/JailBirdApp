require 'spec_helper'

describe GroupsController do
  context "#new" do
    it "passes through the new group thing"

    it "passes through the current user contacts"
  end

  context "#create" do

    it "will flash an error if the user did not save"

    it "will redirect to the contacts path index page?"

  end

  context "#edit" do
    it "will return the right group to edit"

    it "it will display the right view?"
  end

  context "#show" do
    it "will show the right group"

    it "it will render the right view"
  end

  context "#update" do
  end

end