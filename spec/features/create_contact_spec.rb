require 'spec_helper'

feature "Visitor creates contact" do

  scenario "with valid contact info", js: true do
		pending
  end

  scenario "with invalid contact info", js: true do
  	pending
  end

end
